<?php

namespace {namespace};

use LaravelEasyRepository\Service;
use {repositoryInterfaceNamespace}\{repositoryInterfaceName};

class {serviceName} extends Service {

     /**
     * don't change $this->mainInterface variable name
     * because used in extends service class
     */
     protected $mainInterface;

    public function __construct({repositoryInterfaceName} $mainInterface)
    {
      $this->mainInterface = $mainInterface;
    }

    // Define your custom methods :)
}
